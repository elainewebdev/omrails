<!DOCTYPE html>
<html>
  <head>
    <title>Omrails_Elaine</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", media: 'all', "data-turbolinks-track": "reload" %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-dark bg-inverse">
      <div class="container">
        <%= link_to "One month tails", root_path, class: "navbar-brand" %>
        <ul class="nav navbar-nav">
          <li class="nav-item"><%= link_to "Home", root_path, class:"nav-link" %></li>
          <li class="nav-item"><%= link_to "About", about_path, class:"nav-link" %></li>
          <li class="nav-item"><%= link_to "Contact_us", contact_us_path, class:"nav-link" %></li>
          <% if user_signed_in? %>
            <li class="nav-item pull-md-right"><%= link_to "Log out", signout_path, method: :delete, class:"nav-link" %></li>
          <% else %>
            <li class="nav-item pull-md-right"><%= link_to "Sign up", signup_path, class:"nav-link btn btn-primary" %></li>
            <li class="nav-item pull-md-right"><%= link_to "Sign in", signin_path, class:"nav-link" %></li>
          <% end %>
        </ul>
      </div>
    </nav>

    <% flash.each do |name, message| %>
      <%= content_tag(:div, message, class: "alert alert-info #{name}") %>
    <% end %>

    <%= yield %>

    <footer class="small text-muted">
      <div class="container">
      <hr>
        <p style="text-align: center;">ⓒ 2023 One Month</p>
    </div>
    </footer>
  </body>
</html>
